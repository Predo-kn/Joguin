package entities.classes;

public non-sealed class Warrior extends Classes {
    public Warrior(double vida, double dano, double escudo, double attackSpeed, double moveSpeed) {
        super(vida, dano, escudo, attackSpeed, moveSpeed);
    }
}
