package entities.classes;

public non-sealed class Bandido extends Classes {
    public Bandido(double vida, double dano, double escudo, double attackSpeed, double moveSpeed) {
        super(vida, dano, escudo, attackSpeed, moveSpeed);
    }
}
